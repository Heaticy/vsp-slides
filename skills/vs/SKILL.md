---
name: vs
description: VSP Slides 的简写入口。用于通过 vs:plan、vs:start、vs:resume、vs:generate、vs:render、vs:audit、vs:theme、vs:assets、vs:export、vs:polish 快速触发对应的 vsp-slides 子流程。
---

# VS

`vs` 是 `vsp-slides` 的简写命名空间。

## 路由

- `vs:plan` -> `vsp-slides:plan`
- `vs:start` -> `vsp-slides:start`
- `vs:resume` -> `vsp-slides:resume`
- `vs:generate` -> `vsp-slides:generate`
- `vs:render` -> `vsp-slides:render`
- `vs:audit` -> `vsp-slides:audit`
- `vs:theme` -> `vsp-slides:theme`
- `vs:assets` -> `vsp-slides:assets`
- `vs:export` -> `vsp-slides:export`
- `vs:polish` -> `vsp-slides:polish`

执行时读取 `vsp-slides` 对应子 skill 的完整说明。
